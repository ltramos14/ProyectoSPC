export class Vehicle {   
    id?: string;
    vehicleType: string;
    licensePlate: string;
    maxCapacity: number;
    characteristics: string;
    image: string;

    constructor(vehicle?: Vehicle) {
    }
}